import Store from '../../models/Store';

const tag = '[random]';

const RandomView = main => {
  console.log(tag);

  const store = new Store();

  const views = {};

  //   new Controller(store, views);
};

export default RandomView;
